<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <%= @view_module.refresh(@view_template, assigns) %>
    <title>Adventure Time Online</title>
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body class=" bg-gray-50">
    <!-- This example requires Tailwind CSS v2.0+ -->
    <div>
      <nav class="bg-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-16">
            <a href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white py-2 rounded-md text-xl font-semibold">
              Adventure Time Online
            </a>
          </div>
        </div>
      </nav>

      <main>
        <header class="bg-white shadow-sm">
          <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <h1 class="text-lg leading-6 font-semibold text-gray-900">
              <%= @view_module.header(@view_template, assigns) %>
            </h1>
          </div>
        </header>
        <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
          <!-- Replace with your content -->
          <div class="px-4 py-4 sm:px-0 h-96">
            <p class="text-blue-500" role="alert"><%= get_flash(@conn, :info) %></p>
            <p class="text-red-500" role="alert"><%= get_flash(@conn, :error) %></p>
            <%= @inner_content %>
          </div>
          <!-- /End replace -->
        </div>
      </main>
    </div>
  </body>
</html>
